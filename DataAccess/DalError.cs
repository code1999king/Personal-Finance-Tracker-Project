

namespace DataAccess
{
    /// <summary>
    /// Represents the error codes returned by the Data Access Layer (DAL).
    /// </summary>
    public enum DalError
    {
        NotFound = 0,
        Error = 1,
    }
}
